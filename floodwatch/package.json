{
  "name": "floodwatch",
  "version": "0.2.0",
  "private": true,
  "devDependencies": {
    "node-sass": "^3.13.0",
    "react-scripts": "^0.7.0"
  },
  "dependencies": {
    "react": "^15.3.2",
    "react-dom": "^15.3.2",
    "react-router": "^2.8.1",
    "whatwg-fetch": "^1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "yarn sass-build && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",

    "sass-watch": "node-sass --watch --recursive src/sass -o src/css",
    "sass-build":  "node-sass --recursive src/sass -o src/css"
  }
}
